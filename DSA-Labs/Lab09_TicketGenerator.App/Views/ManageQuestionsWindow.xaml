<Window x:Class="Lab09_TicketGenerator.App.Views.ManageQuestionsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Управление вопросами" Height="500" Width="700"
        WindowStartupLocation="CenterOwner">

    <DockPanel Margin="10">
        <!-- Панель управления -->
        <StackPanel DockPanel.Dock="Top" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Добавить вопрос" Click="AddQuestion_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Редактировать" Click="EditQuestion_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Удалить" Click="DeleteQuestion_Click" Margin="0,0,10,0" Padding="10,5"/>
            <Button Content="Обновить" Click="Refresh_Click" Padding="10,5"/>
        </StackPanel>

        <!-- Список вопросов -->
        <DataGrid x:Name="QuestionsGrid" DockPanel.Dock="Top" 
                  AutoGenerateColumns="False" CanUserAddRows="False"
                  SelectionMode="Single" SelectionUnit="FullRow">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="40"/>
                <DataGridTextColumn Header="Текст" Binding="{Binding Text}" Width="*"/>
                <DataGridTextColumn Header="Тип" Binding="{Binding Type}" Width="80"/>
                <DataGridTextColumn Header="Тема" Binding="{Binding Topic}" Width="120"/>
                <DataGridTextColumn Header="Сложность" Binding="{Binding Difficulty}" Width="70"/>
                <DataGridCheckBoxColumn Header="Активен" Binding="{Binding IsActive}" Width="60"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</Window>